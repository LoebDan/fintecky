import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PassrideschedPage } from './passridesched';

@NgModule({
  declarations: [
    // PassrideschedPage,
  ],
  imports: [
    IonicPageModule.forChild(PassrideschedPage),
  ],
})
export class PassrideschedPageModule {}
