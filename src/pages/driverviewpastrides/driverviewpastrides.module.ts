import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DriverviewpastridesPage } from './driverviewpastrides';

@NgModule({
  declarations: [
    // DriverviewpastridesPage,
  ],
  imports: [
    IonicPageModule.forChild(DriverviewpastridesPage),
  ],
})
export class DriverviewpastridesPageModule {}
